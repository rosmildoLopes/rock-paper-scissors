const weapon = ["Rock", "Paper", "Scissors"];

getComputerChoice = () => {
  const computerRandom = weapon[Math.floor(Math.random() * weapon.length)];
  return computerRandom;
};

playRounds = (playerSelection, computerSelection) => {
  switch 
}
